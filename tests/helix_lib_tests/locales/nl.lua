--- Dutch test locale (partial — used to test fallback behaviour)
return {
    test_greeting = 'Hallo, %s!',
    test_simple = 'Dit is een test.',
    -- test_number intentionally omitted — should fall back to English
    -- test_multi intentionally omitted — should fall back to English
}
